import { HandlerModule } from "../types";
export declare class ManifestService {
    private handlers;
    constructor(handlers: Record<string, HandlerModule>);
    load(filePath: string): any;
    createTemplate(serviceName: string, slug: string): string;
    save(filePath: string, content: string, options?: {
        overwrite?: boolean;
    }): void;
    private createFullSchema;
}
