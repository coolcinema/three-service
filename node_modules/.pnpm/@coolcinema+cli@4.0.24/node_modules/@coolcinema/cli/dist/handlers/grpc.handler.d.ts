import { z } from "zod";
import { BaseHandler } from "./base.handler";
import { PushContext, HandlerResult } from "../types";
declare const GrpcInterfaceSchema: z.ZodObject<{
    files: z.ZodArray<z.ZodString>;
    port: z.ZodNumber;
}, z.core.$strict>;
type GrpcItem = z.infer<typeof GrpcInterfaceSchema>;
export declare class GrpcHandler extends BaseHandler<GrpcItem> {
    schema: z.ZodRecord<z.ZodString, z.ZodObject<{
        files: z.ZodArray<z.ZodString>;
        port: z.ZodNumber;
    }, z.core.$strict>>;
    defaults: {
        main: {
            files: string[];
            port: number;
        };
    };
    protected processItem(ctx: PushContext, key: string, item: GrpcItem, result: HandlerResult): Promise<void>;
}
export {};
