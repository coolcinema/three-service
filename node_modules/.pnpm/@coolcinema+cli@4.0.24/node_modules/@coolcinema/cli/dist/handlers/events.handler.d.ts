import { z } from "zod";
import { BaseHandler } from "./base.handler";
import { PushContext, HandlerResult } from "../types";
declare const EventsInterfaceSchema: z.ZodObject<{
    spec: z.ZodString;
}, z.core.$strict>;
type EventsItem = z.infer<typeof EventsInterfaceSchema>;
export declare class EventsHandler extends BaseHandler<EventsItem> {
    schema: z.ZodRecord<z.ZodString, z.ZodObject<{
        spec: z.ZodString;
    }, z.core.$strict>>;
    defaults: {
        main: {
            spec: string;
        };
    };
    push(ctx: PushContext, rawConfig: any): Promise<HandlerResult>;
    protected processItem(): Promise<void>;
}
export {};
