export interface ServiceContract {
    files: string[];
    port: number;
}
export interface ServiceDef {
    slug: string;
    host: string;
    grpc?: Record<string, ServiceContract>;
    http?: Record<string, {
        spec: string;
        port: number;
    }>;
}
export type RegistryType = Record<string, ServiceDef>;
export declare const Registry: RegistryType;
export declare function getGrpcAddress(service: ServiceDef, interfaceName?: string): string;
