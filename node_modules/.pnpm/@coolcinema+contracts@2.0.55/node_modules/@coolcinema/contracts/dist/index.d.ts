export interface ServiceContract {
    files: string[];
    port: number;
}
export interface ServiceDef {
    slug: string;
    host: string;
    grpc?: Record<string, ServiceContract>;
    http?: Record<string, {
        spec: string;
        port: number;
    }>;
    events?: any;
}
export interface InfrastructureConfig {
    rabbitmq: {
        uri: string;
        exchanges?: string[];
    };
    jaeger: {
        endpoint: string;
    };
}
export interface RegistryConfig {
    services: Record<string, ServiceDef>;
    infrastructure: InfrastructureConfig;
}
export declare const Registry: RegistryConfig;
export declare function getGrpcAddress(service: ServiceDef, interfaceName?: string): string;
