"use strict";
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || (function () {
    var ownKeys = function(o) {
        ownKeys = Object.getOwnPropertyNames || function (o) {
            var ar = [];
            for (var k in o) if (Object.prototype.hasOwnProperty.call(o, k)) ar[ar.length] = k;
            return ar;
        };
        return ownKeys(o);
    };
    return function (mod) {
        if (mod && mod.__esModule) return mod;
        var result = {};
        if (mod != null) for (var k = ownKeys(mod), i = 0; i < k.length; i++) if (k[i] !== "default") __createBinding(result, mod, k[i]);
        __setModuleDefault(result, mod);
        return result;
    };
})();
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.GenEventsCommand = void 0;
const child_process_1 = require("child_process");
const fs = __importStar(require("fs"));
const path = __importStar(require("path"));
const config_1 = require("../config");
const chalk_1 = __importDefault(require("chalk"));
class GenEventsCommand {
    async execute() {
        const srcDir = config_1.CONFIG.PATHS.LOCAL_CONTRACTS.EVENTS;
        const absPath = path.resolve(srcDir);
        if (!fs.existsSync(absPath))
            return;
        const files = fs.readdirSync(absPath).filter((f) => f.endsWith(".proto"));
        if (files.length === 0) {
            console.log(chalk_1.default.gray(`Skipping Events: no .proto files in ${srcDir}`));
            return;
        }
        try {
            console.log("🔨 Generating Events types...");
            (0, child_process_1.execSync)(`pnpm exec buf generate --path ${srcDir}`, { stdio: "inherit" });
            console.log("✅ Events types generated.");
        }
        catch (e) {
            console.error("❌ Failed to generate events.");
        }
    }
}
exports.GenEventsCommand = GenEventsCommand;
//# sourceMappingURL=gen-events.command.js.map