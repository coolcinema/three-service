"use strict";
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || (function () {
    var ownKeys = function(o) {
        ownKeys = Object.getOwnPropertyNames || function (o) {
            var ar = [];
            for (var k in o) if (Object.prototype.hasOwnProperty.call(o, k)) ar[ar.length] = k;
            return ar;
        };
        return ownKeys(o);
    };
    return function (mod) {
        if (mod && mod.__esModule) return mod;
        var result = {};
        if (mod != null) for (var k = ownKeys(mod), i = 0; i < k.length; i++) if (k[i] !== "default") __createBinding(result, mod, k[i]);
        __setModuleDefault(result, mod);
        return result;
    };
})();
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.InitCommand = void 0;
const fs = __importStar(require("fs"));
const path = __importStar(require("path"));
const chalk_1 = __importDefault(require("chalk"));
const config_1 = require("../config");
class InitCommand {
    constructor(manifestService, scaffoldService) {
        this.manifestService = manifestService;
        this.scaffoldService = scaffoldService;
    }
    async execute() {
        const targetPath = path.join(process.cwd(), config_1.CONFIG.PATHS.MANIFEST);
        if (fs.existsSync(targetPath)) {
            console.error(chalk_1.default.red(`❌ ${config_1.CONFIG.PATHS.MANIFEST} already exists`));
            process.exit(1);
        }
        const dirName = path.basename(process.cwd());
        const name = this.toPascalCase(dirName);
        // 1. Manifest
        const content = this.manifestService.createTemplate(name, dirName);
        this.manifestService.save(targetPath, content);
        // 2. Structure & Config
        this.scaffoldService.createStructure();
        this.scaffoldService.createBufConfig();
        console.log(chalk_1.default.green("✅ Service initialized with Proto-First structure."));
    }
    toPascalCase(str) {
        return str
            .replace(/(?:^\w|[A-Z]|\b\w)/g, (w) => w.toUpperCase())
            .replace(/[\s\-_]+/g, "");
    }
}
exports.InitCommand = InitCommand;
//# sourceMappingURL=init.command.js.map