export interface FileToUpload {
    path: string;
    content: string;
}
export declare class GitHubService {
    private octokit;
    constructor();
    getFileContent(path: string): Promise<string | null>;
    createAtomicCommit(files: FileToUpload[], message: string): Promise<string>;
}
