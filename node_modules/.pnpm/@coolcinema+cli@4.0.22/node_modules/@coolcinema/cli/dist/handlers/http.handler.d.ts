import { z } from "zod";
import { BaseHandler } from "./base.handler";
import { PushContext, HandlerResult } from "../types";
declare const HttpInterfaceSchema: z.ZodObject<{
    port: z.ZodNumber;
    spec: z.ZodOptional<z.ZodString>;
}, z.core.$strict>;
type HttpItem = z.infer<typeof HttpInterfaceSchema>;
export declare class HttpHandler extends BaseHandler<HttpItem> {
    schema: z.ZodRecord<z.ZodString, z.ZodObject<{
        port: z.ZodNumber;
        spec: z.ZodOptional<z.ZodString>;
    }, z.core.$strict>>;
    defaults: {
        api: {
            port: number;
            spec: string;
        };
    };
    protected processItem(ctx: PushContext, key: string, item: HttpItem, result: HandlerResult): Promise<void>;
}
export {};
