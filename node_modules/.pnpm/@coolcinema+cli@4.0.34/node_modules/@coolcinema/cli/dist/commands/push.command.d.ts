import { ICommand } from "./base.command";
import { RegistryService } from "../services/registry.service";
import { InfraService } from "../services/infra.service";
import { ManifestService } from "../services/manifest.service";
import { HandlerModule } from "../types";
import { GitHubService } from "../services/github.service";
export declare class PushCommand implements ICommand {
    private manifestService;
    private registryService;
    private infraService;
    private gh;
    private handlers;
    constructor(manifestService: ManifestService, registryService: RegistryService, infraService: InfraService, gh: GitHubService, handlers: Record<string, HandlerModule>);
    execute(): Promise<void>;
}
