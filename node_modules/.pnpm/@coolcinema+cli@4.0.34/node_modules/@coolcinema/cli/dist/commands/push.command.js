"use strict";
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || (function () {
    var ownKeys = function(o) {
        ownKeys = Object.getOwnPropertyNames || function (o) {
            var ar = [];
            for (var k in o) if (Object.prototype.hasOwnProperty.call(o, k)) ar[ar.length] = k;
            return ar;
        };
        return ownKeys(o);
    };
    return function (mod) {
        if (mod && mod.__esModule) return mod;
        var result = {};
        if (mod != null) for (var k = ownKeys(mod), i = 0; i < k.length; i++) if (k[i] !== "default") __createBinding(result, mod, k[i]);
        __setModuleDefault(result, mod);
        return result;
    };
})();
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.PushCommand = void 0;
const chalk_1 = __importDefault(require("chalk"));
const config_1 = require("../config");
const error_handler_1 = require("../utils/error-handler");
const path = __importStar(require("path"));
const fs = __importStar(require("fs"));
class PushCommand {
    constructor(manifestService, registryService, infraService, gh, handlers) {
        this.manifestService = manifestService;
        this.registryService = registryService;
        this.infraService = infraService;
        this.gh = gh;
        this.handlers = handlers;
    }
    async execute() {
        let manifest;
        try {
            // 1. Load Manifest
            manifest = this.manifestService.load(config_1.CONFIG.PATHS.MANIFEST);
        }
        catch (e) {
            error_handler_1.ErrorHandler.handle(e, this.handlers);
            return; // Stop execution
        }
        const { metadata, ...sections } = manifest;
        console.log(chalk_1.default.blue(`🚀 Pushing ${metadata.name}...`));
        // ... (Context creation same as before)
        const filesQueue = [];
        const context = {
            serviceSlug: metadata.slug,
            async readFile(p) {
                const fullPath = path.resolve(p);
                if (!fs.existsSync(fullPath))
                    throw new Error(`File not found: ${fullPath}`);
                return fs.readFileSync(fullPath, "utf8");
            },
            uploadFile(p, c) {
                filesQueue.push({ path: p, content: c });
            },
        };
        // 3. Fetch Registry
        await this.registryService.fetch();
        const interfacesData = {};
        // 4. Process Handlers
        for (const [key, config] of Object.entries(sections)) {
            if (key === "version")
                continue;
            const handler = this.handlers[key];
            if (handler) {
                console.log(`Processing ${key}...`);
                try {
                    const result = await handler.push(context, config);
                    interfacesData[key] = result.registryData;
                    if (result.appConfig) {
                        this.infraService.add(result.appConfig);
                    }
                }
                catch (err) {
                    console.error(chalk_1.default.red(`❌ Error processing ${key}:`), err.message);
                    process.exit(1);
                }
            }
        }
        // ... (Finalize & Commit same as before)
        this.registryService.updateService(metadata.slug, metadata, interfacesData);
        filesQueue.push(this.registryService.getArtifact());
        filesQueue.push(this.infraService.createArtifact(metadata));
        try {
            const sha = await this.gh.createAtomicCommit(filesQueue, `fix(registry): update ${metadata.slug}`);
            console.log(chalk_1.default.green(`✅ Success! Commit: ${sha}`));
        }
        catch (e) {
            console.error(chalk_1.default.red("❌ Failed:"), e.message);
            process.exit(1);
        }
    }
}
exports.PushCommand = PushCommand;
//# sourceMappingURL=push.command.js.map