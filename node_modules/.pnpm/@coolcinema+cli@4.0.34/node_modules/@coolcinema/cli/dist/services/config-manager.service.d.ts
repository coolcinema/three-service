export declare class ConfigManager {
    private rootDir;
    private backupPath;
    private configPath;
    constructor(rootDir: string);
    prepare(): boolean;
    restore(): void;
    private hasProtoFiles;
}
