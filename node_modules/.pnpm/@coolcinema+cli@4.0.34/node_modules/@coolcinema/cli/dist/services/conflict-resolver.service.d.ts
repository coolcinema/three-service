export interface IConflictResolver {
    resolve(rootDir: string): Promise<void>;
}
export declare class NodeModulesConflictResolver implements IConflictResolver {
    resolve(rootDir: string): Promise<void>;
    private getServiceSlug;
    private cleanDirectory;
}
