import { z } from "zod";
import { HandlerModule, HandlerResult, PushContext } from "../types";
export declare abstract class BaseHandler<TItem> implements HandlerModule {
    abstract schema: z.ZodTypeAny;
    abstract defaults?: any;
    push(ctx: PushContext, rawConfig: any): Promise<HandlerResult>;
    protected abstract processItem(ctx: PushContext, key: string, item: TItem, result: HandlerResult): Promise<void>;
}
