import { GitHubService } from "./github.service";
export declare class RegistryService {
    private gh;
    private data;
    constructor(gh: GitHubService);
    fetch(): Promise<void>;
    getServices(): Record<string, any>;
    updateService(slug: string, metadata: any, interfaces: Record<string, any>): void;
    loadFromLocal(): void;
    getArtifact(): {
        path: string;
        content: string;
    };
}
