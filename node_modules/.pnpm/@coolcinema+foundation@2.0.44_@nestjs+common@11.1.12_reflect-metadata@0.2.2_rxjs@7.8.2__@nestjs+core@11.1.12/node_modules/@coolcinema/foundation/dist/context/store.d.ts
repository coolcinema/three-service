import { AsyncLocalStorage } from "async_hooks";
export interface AppContext {
    traceId?: string;
    headers: Record<string, string | string[]>;
}
export declare const contextStorage: AsyncLocalStorage<AppContext>;
export declare const runInContext: <T>(ctx: AppContext, fn: () => T) => T;
export declare const getCurrentContext: () => AppContext | undefined;
