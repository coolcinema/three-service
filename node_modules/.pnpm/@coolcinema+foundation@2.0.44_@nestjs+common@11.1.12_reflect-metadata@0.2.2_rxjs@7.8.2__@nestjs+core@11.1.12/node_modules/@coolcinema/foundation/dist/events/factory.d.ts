import { AmqpTransport } from "./amqp";
interface ProtoDefinition {
    name: string;
    methods: Record<string, {
        name: string;
    }>;
}
export declare function createPublisher<Def extends ProtoDefinition>(transport: AmqpTransport, definition: Def, prefix?: string): Record<keyof Def["methods"], (payload: any) => Promise<void>>;
export declare function createConsumer(transport: AmqpTransport, myServiceSlug: string): {
    subscribe: <Def extends ProtoDefinition>(definition: Def, methodName: keyof Def["methods"], sourcePrefix: string, handler: (msg: any, ctx: any) => Promise<void>) => Promise<void>;
};
export {};
