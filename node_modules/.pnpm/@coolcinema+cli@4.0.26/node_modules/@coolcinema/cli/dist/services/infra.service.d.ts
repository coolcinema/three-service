import { AppConfig } from "../types";
export declare class InfraService {
    private config;
    add(partial: AppConfig): void;
    createArtifact(metadata: any): {
        path: string;
        content: string;
    };
}
