import { IRegistry } from "../types";
export interface AmqpConfig {
    url?: string;
}
export declare class AmqpTransport {
    private connection;
    private channel;
    constructor(registry: IRegistry, config?: AmqpConfig);
    getChannel(): import("amqp-connection-manager/dist/types/ChannelWrapper").default;
    close(): Promise<void>;
}
