import { AppContext } from "./store";
export type HeaderSetter = (key: string, values: string[]) => void;
export declare const Propagator: {
    inject(ctx: AppContext | undefined, setHeader: HeaderSetter): void;
};
