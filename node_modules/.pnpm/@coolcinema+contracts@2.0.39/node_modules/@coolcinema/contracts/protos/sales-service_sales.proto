syntax = "proto3";
package coolcinema.sales.rpc;

service SalesService {
  rpc CalculatePrice (CalculatePriceRequest) returns (CalculatePriceResponse);
}

message CalculatePriceRequest {
  string showtime_id = 1;
}

message CalculatePriceResponse {
  double amount = 1;
  string currency = 2;
}
