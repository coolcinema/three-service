export interface GenerateSpecOptions {
    title: string;
    version: string;
    out?: string;
}
export declare function generateSpecFile(routes: Record<string, any>, options: GenerateSpecOptions): void;
